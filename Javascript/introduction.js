var qty = 20;
var price = 30;
var total = qty * price;

var display = "Total  = " + total.toFixed(4) + "$";
var display1 = `Total  = ${total.toFixed(4)} $`

console.log(display);
console.log(display1)